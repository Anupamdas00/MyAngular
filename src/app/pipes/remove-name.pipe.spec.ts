import { RemoveNamePipe } from './remove-name.pipe';

describe('RemoveNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNamePipe();
    expect(pipe).toBeTruthy();
  });
});
